<%= form_for(@periode) do |f| %>
  <% if @periode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@periode.errors.count, "error") %> prohibited this periode from being saved:</h2>

      <ul>
      <% @periode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
